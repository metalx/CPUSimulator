"ClassProjectDescription.doc" is this course project requirement.


For test use.
"testMul.txt" is used to test instructions:  MUL, DIV, TER, AND, ORR, NOT 
"testJump.txt" is used to test instructions:  JZ, JNE, JCC, JMP, JSR, RFS, SOB
"testAdd.txt" is used to test instructions : AMR, SMR, AIR, SIR
"program1.txt" is a program used to select the closest number from 20 numbers with a given number.
"program2.txt" is a program to get strings that include given string from 10 inputted strings.
You can set the value by yourself by using the SetValue button.
